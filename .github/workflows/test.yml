name: Test

on:
  pull_request:
    branches: [ "main" ]
    paths-ignore:
      - README.md
      - LICENSE
      - "*.md"
      - .gitignore
  push:
    branches: [ "main" ]
    paths-ignore:
      - README.md
      - LICENSE
      - "*.md"
      - .gitignore
  workflow_dispatch:

concurrency:
  group: test-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: ${{ matrix.platform }}
    strategy:
      fail-fast: false
      matrix:
        platform: [macos, ios, visionos]
    runs-on: macos-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v5

    - name: Select Xcode 26
      run: sudo xcode-select -s /Applications/Xcode_26.1.app

    - name: Build
      run: make build-${{ matrix.platform }}

    - name: Test
      run: make test-${{ matrix.platform }}

  example:
    name: Example (${{ matrix.platform }})
    strategy:
      fail-fast: false
      matrix:
        platform: [iOS, macOS, visionOS]
        include:
          - platform: iOS
            destination: 'generic/platform=iOS Simulator'
            sdk: iphonesimulator
          - platform: macOS
            destination: 'platform=macOS'
            sdk: macosx
            code_sign: 'CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO'
          - platform: visionOS
            destination: 'generic/platform=visionOS Simulator'
            sdk: xrsimulator
    runs-on: macos-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v5

    - name: Select Xcode 26
      run: sudo xcode-select -s /Applications/Xcode_26.1.app

    - name: Build Example for ${{ matrix.platform }}
      run: |
        xcodebuild build \
          -project Example/Example.xcodeproj \
          -scheme Example \
          -configuration Debug \
          -sdk ${{ matrix.sdk }} \
          -destination '${{ matrix.destination }}' \
          ${{ matrix.code_sign || '' }}
